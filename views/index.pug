extends layout.pug

block content

  article.portfolio-intro

    h1 My Portfolio

    p This is a site to display all of the projects that I have worked on


  article.portfolio-project-thumbs
    .projects-box.box.grid

      each project in projects 

        .cell
          a(href='./projects/' + project.id)
            img.thumbnail(src=project.image_urls[0], alt='Thmbnail ' + project.project_name)
            h5 #{project.project_name}

